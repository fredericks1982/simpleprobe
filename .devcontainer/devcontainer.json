{
  "name": "Simple Probe",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "remoteUser": "probe",
  "forwardPorts": [
    9090
  ],
  "runArgs": [
    "--env-file",
    ".env",
    "-p",
    "9090:9090"
  ],
  "postStartCommand": "nohup python server.py > /tmp/probe.log 2>&1 & echo $! > /tmp/probe.pid && echo '✔ Probe is running on port '${SIMPLEPROBE_PORT:-9090}' (PID '$(cat /tmp/probe.pid)')' ",
  "customizations": {
    "vscode": {
      "extensions": [
        "anthropic.claude-code",
        "ms-azuretools.vscode-docker"
      ],
      "settings": {
        "terminal.integrated.shellIntegration.enabled": true
      }
    }
  }
}