{
  "name": "Simple Probe",
  "build": { "dockerfile": "Dockerfile" },
  "remoteUser": "probe",
  "forwardPorts": [1982],
  "runArgs": ["--env-file", ".env"],
  "postStartCommand": "nohup python server.py > /tmp/probe.log 2>&1 & echo $! > /tmp/probe.pid && echo '✔ Probe is running on port '${PORT:-1982}' (PID '$(cat /tmp/probe.pid)')' && echo '' && printf 'Press Enter to close this terminal (the probe keeps running)...' && read _ && echo ''",
  "customizations": {
    "vscode": {
      "extensions": ["anthropic.claude-code", "ms-azuretools.vscode-docker"]
    }
  }
}
